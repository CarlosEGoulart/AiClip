# Default (empty): the app calls same-origin /api/v1/health through the Vite /api proxy.
# Optional public API origin (no trailing slash) used as a prefix, e.g. https://api.example.com.
# A separate production origin requires deployment-owned CORS/routing; the proxy applies to local dev only.
VITE_API_BASE_URL=
