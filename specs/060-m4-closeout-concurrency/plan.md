# Implementation Plan: Existing Issue #60

## Planning review and ownership

This revision independently vets the live #60 issue against root AGENTS.md, API addendum, all four agent role definitions, current #60 bundle, historical #58 spec/plan/test-plan only, current model/migration/job/action/validator/configuration, job tests/TestCase/phpunit.xml/MinIO conventions/backend CI, README, project-state, roadmap, architecture, PRD and ADR-0002. No historical evidence was rewritten or treated as new proof. No shell, Git lifecycle, real `.env` inspection, or implementation execution was performed by Planner.

The active issue is cohesive M4 closeout, consistent with the PRD/ADR boundaries and the user's nine-scenario matrix. Prior issue creation bypassed failed Phase A: this review does not fabricate or retroactively certify Phase A. Orchestrator must record that distinction and verify current readiness before resuming. The existing issue title is retained exactly as in `spec.md`.

The live issue's “M0–M4 completed” denotes the merged M4 **foundation**, not M5 or #60 closure. “Sanitized failure recorded” on contention denotes controlled busy diagnostics, not a mutation of the owner's row. Its nine scenarios are C1–C9; historical malformed-output and worker-timeout cases remain additional required tests. These interpretations clarify the existing body without requiring a new issue or scope change; no issue-body edit is required by this revision.

Planner edits only this bundle. Builder owns scoped application code/tests and actual evidence. Orchestrator alone owns the four documentation edits and lifecycle. Tester executes independently and writes findings, never repairs code. Honor tool permissions: unavailable/denied execution goes back to Orchestrator/human, never through a command-wrapper workaround or control-plane edit.

## Prerequisite gates — before execution

1. Orchestrator rechecks sole open #60, no conflicting PR/lifecycle, supplied branch/head and untracked bundle, and #58/#59 closed/merged status. Do not recreate an issue, branch or planning directory.
2. Verify a currently available **free Builder model** and runtime access using authorized tooling; do not assume a named model is available/free or silently substitute paid access. Verify independent Tester availability, preferably a different model. Missing access blocks delegation, not a reason to bypass roles.
3. Obtain explicit #60 authorization identifying a **disposable PostgreSQL database**, host/instance and allowed provisioning/migration/cleanup. `aiclip_test_issue58` authorization applied only to #58; neither reuse of it nor existence/authorization of an issue60 database may be assumed. If absent, stop for human/operator provision or approval. No production migrations, DB resets or shared-data cleanup.
4. Before any destructive command or suite that migrates/refreshes/truncates, verify PHP `pdo_pgsql`, effective Laravel/PDO PostgreSQL driver, successful `SELECT 1`, and `current_database()` matching the explicitly authorized target. Repeat in child test processes; include safe target/driver checks in the harness before fixture cleanup. Merely setting environment variables is insufficient. Never print credentials. Use supplied test-environment overrides; **do not change phpunit.xml**, whose defaults are SQLite/sync.
5. Check PostgreSQL 16, available process spawning and deterministic IPC, PHP/Composer dependencies, Python worker/pytest, FFmpeg/FFprobe and existing real `scenedetect[opencv-headless]`, MinIO/private test bucket, Node toolchain, Chromium and managed-app ports. MinIO access/cleanup must target authorized disposable storage. Queue tests use existing database queue infrastructure, not sync/fake queue. No dependency upgrades or large model downloads.
6. Verify runtime transcription-model availability only if an actual model-backed run is proposed; it is not assumed and no download is authorized here. Existing deterministic transcription coverage and real PySceneDetect integration remain mandatory. Infrastructure/setup failures are blockers, never valid RED or acceptable skips. CI's ephemeral test services are distinct from local authorization.

## Smallest implementation surface

| Owner | Surface and reason |
|---|---|
| Builder | `apps/api/app/Jobs/ProcessMediaAsset.php`: existing clip stage and `failed()` claim/finalization paths, bounded not-ready behavior, stale/deleted/aborted/busy resolution and necessary persisted-probe reuse. Do not redesign upstream stages. |
| Builder | `apps/api/app/Services/ProcessMediaAction.php`, `ClipAnalysisValidator.php`, `config/media.php`: one strict operational timeout source, derived local bound, actual execution metadata and completion validation; remove unused independent setting. Preserve transport/result/scoring behavior. |
| Builder | `MediaClipAnalysis.php` only for required retry clearing/atomic lifecycle guards. Existing unique/cascade migration is sufficient; no schema change planned. |
| Builder | Dedicated `apps/api/tests/Feature/Jobs/ProcessMediaAssetClipAnalysisConcurrencyTest.php` (or equivalent discovered Feature file), scoped `tests/Support` process/IPC helpers and targeted existing application tests. Test-only scripts must not become production Artisan commands. |
| Orchestrator | Exactly `README.md`, `docs/roadmap.md`, `docs/project-state.md`, `docs/architecture.md`, after Builder GREEN/refactor and before Tester. |

Prefer small existing-location corrections; one narrowly necessary operational/claim helper is permissible to avoid duplicate rules, not a large coordinator rewrite. Return any needed broader change to Planner. No worker algorithm change, new migration, infrastructure redesign or CI edit is expected. Historical specs intentionally retain obsolete configuration references; absence checks are limited to active implementation/configuration, not a codebase-wide zero-match assertion.

## Narrow resumption decision — exception boundary and scene fixtures

Planner independently read the live #60 issue, current evidence chronology (including later appended RED authorization), historical #58 readiness/error/lifecycle/finalization requirements, the entire scene test file, current job/action/model/validator and C4 test. Reported GREEN totals are not certification of full coverage. Current job's outer catch returns on unexpected abort; current action catches all Throwable as ProcessMediaException. Both violate the clarified boundary. C4's RuntimeException is a legitimate unexpected abort injection, not an expected process failure; retain it. Its three existing assertions are useful but do not prove exact snapshots, retry delivery, real termination or privacy.

All six reported completion failures are in `apps/api/tests/Feature/Jobs/ProcessMediaAssetSceneDetectionTest.php`. Each legitimately reaches `analyzeClips` exactly once under merged #58; retain every original assertion and upstream expectation:

| Exact test name | Intended pipeline state at clip boundary | Authorized interaction/result |
|---|---|---|
| `chains probe to scene detection to completed` | Stored -> probed, duration 5000; completed scenes [0,3120), [3120,5000); successful extraction/completed transcript [0,1000), [1000,2000) | Once, projected transcript timings; valid full provenance with empty candidates because both scenes are below min=5000. |
| `skips scene detection if analysis exists with completed status` | Stored -> probed, duration 5000; reuse completed scene [0,5000); extraction/transcript complete [0,1000) | Once, transcript used; one eligible whole-scene candidate. No detectScenes call remains mandatory. |
| `video without audio still receives scene detection` | Stored -> probed, duration 5000, no audio; completed scenes [0,3000), [3000,5000) | Once, scene-only, valid empty candidates; preserve no extraction/transcription expectations. |
| `previous probe tests remain green` | Stored -> probed, duration 5000, null audio/video codecs; video_codec key exists and scene stage completes with [] | Once, scene-only, executed valid empty result. Empty completed scenes are ready even with null codec; do not invent a no-video skip. |
| `propagates duration from probe to scene detection contract` | Stored -> probed, duration 7500, no audio; completed scenes [0,3750), [3750,7500) | Once, scene-only, valid empty candidates; retain duration propagation assertion. |
| `uses asset duration_ms when probe already completed` | Initially probed, persisted duration/probe 10000; completed scenes [0,5000), [5000,10000), no audio | Once, scene-only; two eligible whole-scene candidates with deterministic chronological tie-break ranks. Preserve no probe/extract/transcribe calls. |

Use explicit per-test/per-context `once()` expectations and independently specified valid results matching that context's duration, scenes, transcript availability and current configuration. Assert the captured contract's projected inputs and persisted clip completion so a malformed stub cannot pass via sanitized FAILED. Do not reuse the 40000-ms golden response unchanged, derive expectations with production scoring, alter scenes/durations/config to simplify fixtures, or supply generic empty success for eligible scenes.

Proper outer exception propagation also exposes four ready contexts in the same inspected file that currently assert only upstream data: `creates MediaSceneAnalysis on successful detection`, `stores scene detection metadata correctly`, `transcription failure does not block scene detection`, and `previous transcription tests remain green`. Explicit fixture completion is authorized there on the same grounds: the first two have 5000-ms completed split scenes (3120/1880), valid completed transcript and empty candidates; the third has one 5000-ms scene and failed transcript (scene-only, one candidate); the fourth has one 5000-ms scene and completed two-segment transcript (one candidate). Preserve all upstream assertions, including failed transcript. Failed/missing scene contexts must not receive analyzeClips stubs; controlled upstream failure resolves without worker execution. No broad TestCase/beforeEach mock defaults.

Minimal corrective files for this clarification: `apps/api/app/Jobs/ProcessMediaAsset.php` (rollback/outer signaling and safe exhaustion), `apps/api/app/Services/ProcessMediaAction.php` (narrow classified conversion in analyzeClips only), the scene test file above, `apps/api/tests/Feature/Jobs/ProcessMediaAssetConcurrencyMatrixTest.php`, its `apps/api/tests/Support/Issue60MatrixChild.php` only as necessary for abort/queue/termination proof, and targeted boundary tests in `apps/api/tests/Feature/Jobs/ProcessMediaAssetClipAnalysisTest.php` / `apps/api/tests/Unit/Services/ClipAnalysisResultTest.php`. A local helper in the scene file is allowed for constructing full test provenance, with explicit context-specific expectations/results. No new exception class is required: a fixed sanitized abort discriminator in the existing exception type can distinguish propagation from classified failure. Preserve existing validator/config work; no unrelated production edits or changes to TestCase/phpunit.xml, upstream semantics, scoring/ranking or historical specs.

Test-first: add outer-signal/privacy and real-action unexpected-abort assertions, record authentic RED and obtain Orchestrator review before their production correction. Missing mock failures are fixture incompleteness, not production RED. Adjust C4 calls to assert/catch the sanitized exception individually, then execute every existing persistence assertion; never replace them with an early expected-exception annotation. Complete the explicitly listed scene fixtures without weakening assertions. Follow the focused rerun sequence in test-plan before full regressions, documentation and independent Tester. No lifecycle or M5 authorization is added.

Inspection also shows current first-create remains outside the transaction and cleanup is best-effort, whereas the approved bundle already requires atomic creation and real termination proof. Do not reinterpret reported 12/12 as satisfying unexecuted variants or weaken that requirement. Existing claim-hardening scope remains unchanged; any additional production correction needs its own authentic test-first failure and Orchestrator authorization, not opportunistic repair in the fixture pass.

### Follow-up decision: existing sanitization dataset

Independent inspection of `apps/api/tests/Unit/Services/ClipAnalysisResultTest.php:91–109` and `tests/Support/ClipAnalysisFixture.php:60–105` establishes that `process exception` means a directly injected plain PHP RuntimeException, not a simulated classified transport outcome. The fake `run()` simply throws the supplied object; its other branches advertise successful execution. The installed Symfony `Process.php:239–289` distinguishes `run()` (returns exit status) from `mustRun()` (throws ProcessFailedException for nonzero status). The application uses `run()` then `isSuccessful()`. Symfony's own RuntimeException extends PHP RuntimeException, while its specific ProcessTimedOutException extends that Symfony class; `run()` also documents startup, already-running and signal errors. This hierarchy is not a reason to classify the broad base class as expected failure. Neither the fixture nor the dataset constructs a Symfony timeout or demonstrates nonzero exit. #58's classified timeout/nonzero/invalid-JSON rule and unhandled-abort rule therefore support an unexpected-abort expectation here.

Authorize a test-only correction in `apps/api/tests/Unit/Services/ClipAnalysisResultTest.php`: rename the misleading case to identify unexpected process-boundary RuntimeException and give each dataset an explicit expected message (or split the unexpected case into an equally strict test). Keep the actual RuntimeException injection, exact ProcessMediaException type assertion, exact message assertion, null previous exception and empty stderr checks. The four malformed-output cases continue to expect `Clip analysis failed`; the unexpected case must expect exactly `clip_analysis_aborted`. Do not accept either category, delete the case, replace it with a timeout, or merely assert absence of the sentinel. No fixture helper or production edit is necessary for this conflict. Do not widen catches to force the old message.

The reported 4/5 result is a stale classification expectation, not new evidence of production failure. Record the correction honestly; no manufactured production RED is required for aligning this test with the approved boundary. Rerun the sanitization dataset, full result test file, boundary/scene and retained E1/E2 controls, then the already-required regression sequence. Preserve all current Builder work. Atomic first-create and actual termination proof remain outstanding acceptance obligations; cleanup is not atomic rollback. No new lifecycle, infrastructure, dependencies or M5 work is authorized.

### Final scope clarification: eligible-empty model completion

Independent inspection: `ClipAnalysisValidator::result()` validates the request and compares the entire response against `expectedAnalysis()`, whose eligible selection legitimately yields []. `validateCompletion()` instead unconditionally rejects empty candidates before rederivation; `MediaClipAnalysis::markCompleted()` invokes that method before its update. Thus a valid action result can fail persistence. #58 spec (empty-result algorithm example, independent validation and model-boundary requirements) expressly requires legitimate empty completion. This is a scope-required preservation correction in the already-authorized validator, not a new feature or relaxation of fixture expectations.

The **current six eligible-empty failures** differ from the earlier six unstubbed completion tests. Their inspected contexts are:

| Exact scene test | Why the expected eligible set is empty |
|---|---|
| `chains probe to scene detection to completed` | Duration 5000; scenes of 3120 and 1880 ms; completed two-segment transcript. |
| `creates MediaSceneAnalysis on successful detection` | Duration 5000; scenes of 3120 and 1880 ms; completed one-segment transcript. |
| `stores scene detection metadata correctly` | Same short scenes and one-segment completed transcript; scene metadata assertions remain required. |
| `video without audio still receives scene detection` | Duration 5000; scenes of 3000 and 2000 ms; transcript unavailable. |
| `previous probe tests remain green` | Valid duration 5000; completed scenes=[]; transcript unavailable. |
| `propagates duration from probe to scene detection contract` | Duration 7500; two 3750-ms scenes; transcript unavailable. |

All use unchanged min=5000, target=30000, max=60000 and a full context-appropriate `sceneClipEmptyResult` response. Keep their explicit once-only action interaction, exact projected contracts, upstream assertions and COMPLETED/null-error/[] assertions. FAILED would mask the defect. Keep the reduced local metadata helper; no further factory expansion or fixture redesign is required.

Minimal production authorization for this conflict: `apps/api/app/Services/ClipAnalysisValidator.php`, completion boundary only. Replace unconditional nonempty rejection with strict candidate presence/list checks and eligibility-aware validation, using the existing independent request/rederivation/exact-comparison primitives rather than duplicating scoring. For an empty result, validate the **entire** analysis (algorithm/version, exact parameters/configuration/effective weights/transcript availability/policies/limits and []) against independently rederived expectations; then still perform all snapshot/privacy and operational-metadata validation before model update. Do not merely remove the guard: the current required-parameter-key loop does not establish parameter values/equality. No empty-result early return may bypass remaining checks. Preserve existing nonempty validation and rejection coverage; broader validator rewrites or unrelated rank/algorithm corrections are not authorized by this clarification. No model, job, action, worker, schema or config change is needed for this conflict.

Test-first additions belong to `apps/api/tests/Unit/Services/ClipAnalysisCompletionTest.php`, with targeted parity controls in `ClipAnalysisResultTest.php` and the already-corrected scene test assertions retained. The existing completion dataset `empty` modifies the 40000-ms golden response while leaving eligible scenes in its snapshot: it is **fabricated empty**, must remain rejected with zero writes, and is not contradictory. Rename its label only if useful; never remove or reverse its assertion. Its 19/19 reported result omitted legitimate empty-positive coverage and therefore did not prove this contract. Execute direct model eligible-empty RED and negative controls, return actual results to Orchestrator for GREEN authorization, then make the narrow validator correction. The reported six scene failures support this diagnosis but Planner has not executed them or certified the aggregate counts.

After GREEN, rerun completion/result suites, all six cases and full scene suite, clip-analysis/job boundary tests, full #60 concurrency matrix, Pint/post-format tests and full backend regression including real MinIO, followed by the standing full regression/independent review requirements before documentation/Tester handoff. Atomic first-create and actual termination/rollback proof remain required; existing compensating cleanup must never be called atomic. Preserve all other current work and all #58 artifacts; no lifecycle or M5 authorization is added.

## Phase 1 — Tests only, authentic RED returned

1. Build committed synthetic fixtures and child-process harness before production edits. Current job tests attach `RefreshDatabase` per file and base TestCase installs a process mock. Dedicated concurrency tests must explicitly use no outer RefreshDatabase/DatabaseTransactions transaction, replace the default action binding with the scenario's recording boundary, and commit fixtures so child connections can see them. Do not change global TestCase/testing defaults. Teardown targets only this run's fixtures and joins/terminates owned child processes before cleanup.
2. Implement C1–C9 and retained error/timeout cases according to `test-plan.md`; no mocked locks, SQLSTATE injection in place of real timeout, sequential-only concurrency, or fake queue exhaustion proof. Use before-query test instrumentation/IPC where necessary to pause both callers after absent pre-read and before insert, without replacing production claim logic.
3. Add strict timeout/derived metadata/config removal tests. Useful behavioral RED candidates include effective `current_setting('lock_timeout')` inside an actual claim, nondefault valid timeout completion, first-create conflict handling, and real bounded queue exhaustion. Missing barriers themselves are not production behavior and not valid RED. Do not assert in advance that every baseline test must fail; already-correct scenarios are passing controls.
4. Execute on the authorized PostgreSQL environment. Record commands, exit codes, expected/observed assertions and missing behavior, safe environment identity and passing controls in the one #60 `evidence.md`. Repair only tests/environment if failure is setup/syntax/import related.
5. **Stop and return executed RED to Orchestrator. No production correction until Orchestrator reviews authentic behavioral failures and explicitly authorizes GREEN.** Later discovered behaviors likewise require tests first. Never manufacture baseline failures or substitute mutation/fabricated output for actual execution.

## Phase 2 — Minimal correction to GREEN

1. Validate/capture operational timeout once before unsafe work; derive lock wait and use the same attempt values for process execution, database setting and completion metadata. Preserve strict completion checks without comparing history to current ambient configuration. Remove the independent active config key and lookup only. Handle malformed environment input without permissive casts.
2. Apply verified transaction-local PostgreSQL configuration before insert/claim contention. Use the existing unique constraint plus conflict-safe insert and locked reread. Do not prescribe invalid parameterized `SET` syntax. Verify the effective database value, not just a SQL string spy.
3. Recheck asset existence, status and authoritative inputs; serialize all clip write paths, including upstream-controlled failures and queue failure callbacks. Empty locked reread is deleted/no-op, not refresh-on-null or recreation. Expected worker/validation failures commit atomic sanitized failure. Unexpected faults roll back and remain unresolved. Catch PostgreSQL errors outside the failed transaction or recover a savepoint before reread; lock timeout never authorizes owner/asset mutation.
4. Keep upstream work outside the claim. Preserve terminal assets/completed snapshots using fresh locked state where finalization needs it, not stale Eloquent attributes. Reuse persisted valid probe on terminal reruns as required by #58 rather than invoking unpersistable probe results. Do not change unrelated upstream algorithms or error handling.
5. Make actual database-queue attempts implement three-attempt/5-second not-ready policy and safe exhaustion; preserve locked/completed owners and terminal assets. Never persist an unowned analyzing state between attempts. Unexpected rollback recovery and exhaustion use the same claim rules. Do not invent a generic queue coordinator or change global queue config.
6. Run targeted tests after each correction. Existing #58 tests must not be weakened; unreachable assertions after expected throws should be exercised meaningfully, not treated as proof of exhaustion. If unrelated existing defects emerge, report them without opportunistic repair. Scope-required preservation failures block acceptance and go back to Planner if the correction would broaden scope.

## Phase 3 — GREEN, REFACTOR, documentation, Tester

1. Builder executes the complete dedicated suite **three consecutive times** with fresh committed fixtures/child processes, plus the targeted/unit/full regression matrix. Record exact new totals, integration case names, timings and zero mandatory skips/risky tests. Pint and scoped refactor must be followed by reruns; evidence must describe what actually ran, not baseline counts copied forward.
2. Builder returns GREEN/refactor with changed files, scenario mapping, evidence path and any blockers. Orchestrator then edits exactly the four documentation files per spec, including the current-vs-target topology correction. No documentation claim may label #60 merged or assign new proof to #58. This happens **before** Tester reviews the combined final worktree.
3. Tester independently executes the actual dedicated PostgreSQL suite **three consecutive times**, not merely broad Laravel totals, validates persisted states/worker counts/real queue events, runs regressions and reviews documentation, privacy and scope. Tester also exercises existing running UI/API workflows at all three required viewports. No new UI is N/A; application regression review is not N/A.
4. Tester returns APPROVE only when all required behavior/environment checks execute successfully. REJECT routes through Orchestrator to Builder, then Tester; docs defects route to Orchestrator. Changes after approval require applicable revalidation. Keep exactly one #60 evidence file owned by executing roles with `### RED`, `### GREEN`, `### REFACTOR` and independent findings.

## Final lifecycle gate

No commit before Tester APPROVE. Only Orchestrator may commit/push/open the issue-linked PR after approval. Inspect actual final-head Backend, Frontend, E2E, governance and pr-enforcement checks/logs, including discovery/execution of the dedicated tests and real integrations. Stale-head GREEN is insufficient; permissions/environment blockers are reported, not bypassed. Stop at **CI_GREEN_WAITING_HUMAN_MERGE**: no merge-gate invocation (including check mode), merge, closure or next issue. Separate explicit human authorization is required for later lifecycle operations and M5.
