# Implementation Plan — Issue #19

## Baseline and Boundaries

Continue only `@carlosegoulart/19/fix/m1-foundation-stabilization` as reported by the authorized handoff. Planner inspected governance/bootstrap/project state, relevant skills, architecture/PRD excerpts, root/app READMEs, frontend component/test/config/scripts/environment example, backend route/health tests, and frontend/E2E workflow definitions. No shell commands, GitHub fetch, tests, lifecycle operations, or implementation were performed during planning.

Observed gaps: frontend test replaces the whole component with a copy; production already checks `res.ok`; backend already catches query exceptions safely but tests do not exercise that path or actually observe queries; Jest scripts coexist with an unused Vitest config; the ESM static server uses `__dirname` and dist; Playwright permits reused servers; E2E CI starts apps separately; browser tests filter errors broadly and race loading. Documentation is stale. Do not invent missing production defects to claim RED.

## Ordered Implementation

1. **Establish evidence before changes.** Builder verifies scope/runtime prerequisites and applicable nested API rules/skills and version-specific official guidance. Execution roles create the issue evidence artifact required by governance. Run the original frontend suite unmodified; apply a reversible, syntactically valid production health-rendering mutation and show the original suite still passes. Restore it. Run `node test-server.js` standalone with no masking server and record the actual ESM failure. Distinguish these observations from missing dependencies or resource failures.

2. **Restore the frontend runner and test boundary.** Update `apps/web/package.json`, lockfile, Vitest/TypeScript setup, and test setup as needed; remove Jest-only files/dependencies, including ts-jest. Keep Node 24/Vite 8. Limit Vitest discovery to unit/component tests, not Playwright files; use jsdom, proper matcher integration, cleanup, and restored fetch/environment stubs. Replace the copied implementation with a direct production import. Add deferred-fetch pending/success, rejection, non-2xx, and URL tests before related behavior changes. Add `vite/client` types and remove the suppression. Initially passing coverage is explicitly labeled as such. Reapply the original mutation and focused state/status mutations to prove assertion failures, restore, and run GREEN. A runner exception is blocked pending Planner adjudication of reproducible non-resource evidence; do not silently downgrade or substitute.

3. **Protect the existing backend contract.** Extend `apps/api/tests/Feature/HealthTest.php` using the existing suite conventions. Force the query boundary to throw with synthetic sensitive text and assert exact safe 503 JSON. Separately run real PostgreSQL and register query observation before the request, asserting `SELECT 1` actually executes through PostgreSQL along with the successful response. Keep mocks/listeners isolated between tests. Since the route already implements safe behavior, record this as coverage addition; demonstrate regression sensitivity by temporary query-removal and exception-disclosure mutations, then restore. Avoid production-route refactoring without a failing requirement.

4. **Repair same-origin startup as one lifecycle.** Coordinate component URL behavior, `.env.example`, Vite proxy, package scripts, and Playwright configuration. An empty/unset `VITE_API_BASE_URL` means relative `/api/v1/health`; an optional public origin prefixes that path. Document consistent origin formatting. Use explicit loopback hosts and dedicated unused frontend/backend test ports, Vite strict-port behavior, and Laravel no-port-fallback behavior verified for its installed command. Configure Playwright `webServer` for both apps with readiness URLs/timeouts, `reuseExistingServer: false`, correct proxy target/environment inheritance, and cleanup. Remove obsolete static server after reproduction. Keep the application build a separate check, not an E2E startup dependency.

5. **Strengthen actual-stack browser tests.** Update `apps/web/e2e/health.spec.ts` and viewport projects. Use real backend/PostgreSQL success navigation with response assertions; isolate controlled pending/network/HTTP-failure scenarios using narrowly scoped routes. Hold and release requests explicitly rather than sleeping or relying on response timing. Register all diagnostic listeners before navigation. Assert health visibility and geometry/overflow, retain screenshots for all required viewports and states, and account narrowly for intentional negative-path diagnostics. Do not suppress unexpected errors. Apply only minimal UI corrections if criteria genuinely fail.

6. **Align CI and documentation.** E2E CI installs prerequisites, prepares Laravel/PostgreSQL and passes DB environment to the managed server invocation, then runs `npm run test:e2e`; remove separate background servers, invalid/redundant startup settings, and fixed readiness sleeps. Preserve backend/frontend checks and governance workflow/gates. Upload useful screenshots/traces/reports, including successful viewport review artifacts where needed. Replace template app READMEs and stale root content with concise executable foundation setup/test instructions and planned-feature boundaries. Update only relevant architecture runner/config descriptions. Compact project state under its existing six exact headings; record reviewed capability accurately, not future merge/closure. Do not change unrelated roadmap/product scope.

7. **Verify and hand off.** Run targeted suites, full relevant suites, build/lint, governance checks, and clean-start E2E after refactoring. Check no temporary mutation remains. Record command, working directory, versions/environment, exit status, relevant assertion, restoration, and concise artifact references in execution evidence. Independent Tester must reproduce the test-plan checks; Builder may apply temporary mutations under Tester observation if Tester cannot edit production, but Tester runs and judges failures and restored GREEN independently. On rejection, correct only #19 through Orchestrator. Outer Orchestrator alone manages CI/lifecycle, Tester-approved merge, closure verification, and stopping.

## Evidence Discipline

- Separate baseline blind-spot demonstration, startup regression, newly added passing coverage, mutation assertion failures, final GREEN, and refactor reruns.
- Missing modules, unavailable PostgreSQL, syntax/test-fixture errors, or resource exhaustion are setup blockers, not valid behavioral RED.
- The existing standalone ESM defect is genuine startup regression evidence, not a substitute for component/backend assertion-based mutation evidence.
- Do not weaken governance bundle requirements because Planner owns only three artifacts; execution roles supply `evidence.md` before the completed bundle is validated.
